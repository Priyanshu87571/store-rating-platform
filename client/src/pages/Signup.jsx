import {useState} from 'react';import {Link,useNavigate} from 'react-router-dom';import {api} from '../services/api';export default function Signup(){const[f,setF]=useState({name:'',email:'',address:'',password:''}),[error,setError]=useState(''),nav=useNavigate();const change=e=>setF({...f,[e.target.name]:e.target.value});const submit=async e=>{e.preventDefault();setError('');try{await api.post('/auth/signup',f);nav('/login')}catch(e){setError(e.response?.data?.message||e.response?.data?.issues?.[0]?.message||'Registration failed')}};return <div className="auth"><div className="authcard"><h1>Create account</h1><p className="muted">Name must be 20–60 characters.</p>{error&&<div className="error">{error}</div>}<form onSubmit={submit}><label>Name<input name="name" value={f.name} onChange={change} required minLength="20" maxLength="60"/></label><label>Email<input name="email" type="email" value={f.email} onChange={change} required/></label><label>Address<textarea name="address" value={f.address} onChange={change} maxLength="400" required/></label><label>Password<input name="password" type="password" value={f.password} onChange={change} required minLength="8" maxLength="16"/><small>8–16 chars, uppercase + special character</small></label><button className="primary full">Sign Up</button></form><p>Already registered? <Link to="/login">Login</Link></p></div></div>}
