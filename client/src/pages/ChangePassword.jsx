import {useState} from 'react';import {api} from '../services/api';export default function ChangePassword(){const[f,setF]=useState({currentPassword:'',newPassword:''}),[msg,setMsg]=useState(''),[error,setError]=useState('');const submit=async e=>{e.preventDefault();setMsg('');setError('');try{await api.put('/auth/password',f);setMsg('Password updated successfully.')}catch(e){setError(e.response?.data?.message||'Could not update password')}};return <section className="narrow"><h1>Change Password</h1><div className="card"><form onSubmit={submit}>{error&&<div className="error">{error}</div>}{msg&&<div className="success">{msg}</div>}<label>Current Password<input type="password" value={f.currentPassword} onChange={e=>setF({...f,currentPassword:e.target.value})} required/></label><label>New Password<input type="password" value={f.newPassword} onChange={e=>setF({...f,newPassword:e.target.value})} required/><small>8–16 chars, at least one uppercase and one special character.</small></label><button className="primary">Update Password</button></form></div></section>}
