import {useState} from 'react';import {Link,useNavigate} from 'react-router-dom';import {useAuth} from '../context/AuthContext';export default function Login(){const[email,setEmail]=useState(''),[password,setPassword]=useState(''),[error,setError]=useState(''),{login}=useAuth(),nav=useNavigate();const submit=async e=>{e.preventDefault();setError('');try{const u=await login({email,password});nav(u.role==='ADMIN'?'/admin/dashboard':u.role==='STORE_OWNER'?'/owner/dashboard':'/stores')}catch(e){setError(e.response?.data?.message||'Login failed')}};return <div className="auth"><div className="authcard"><h1>Welcome back</h1><p className="muted">Sign in to manage stores and ratings.</p>{error&&<div className="error">{error}</div>}<form onSubmit={submit}><label>Email<input type="email" value={email} onChange={e=>setEmail(e.target.value)} required/></label><label>Password<input type="password" value={password} onChange={e=>setPassword(e.target.value)} required/></label><button className="primary full">Login</button></form><p>New user? <Link to="/signup">Create an account</Link></p><div className="demo"><b>Demo accounts</b><br/>Admin: admin@example.com<br/>User: user@example.com<br/>Owner: owner@example.com<br/>Password: Password@123</div></div></div>}
