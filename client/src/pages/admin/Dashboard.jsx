import {useEffect,useState} from 'react';import {api} from '../../services/api';export default function Dashboard(){const[d,setD]=useState(null);useEffect(()=>{api.get('/admin/dashboard').then(r=>setD(r.data))},[]);return <section><h1>Admin Dashboard</h1><p className="muted">Platform overview</p><div className="stats">{[['Total Users',d?.totalUsers],['Total Stores',d?.totalStores],['Submitted Ratings',d?.totalRatings]].map(([a,b])=><div className="stat" key={a}><span>{a}</span><strong>{b??'—'}</strong></div>)}</div><div className="card"><h2>Administrator access</h2><p>Create users and stores, search and sort records, and inspect user details from the navigation above.</p></div></section>}
