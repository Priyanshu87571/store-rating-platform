import {Link,useNavigate} from 'react-router-dom';import {useAuth} from '../context/AuthContext';export default function Navbar(){const{user,logout}=useAuth(),nav=useNavigate();const out=async()=>{await logout();nav('/login')};return <nav><Link className="brand" to={user?.role==='ADMIN'?'/admin/dashboard':user?.role==='STORE_OWNER'?'/owner/dashboard':'/stores'}>RateHub</Link><div className="navlinks">{user?.role==='ADMIN'&&<><Link to="/admin/dashboard">Dashboard</Link><Link to="/admin/users">Users</Link><Link to="/admin/stores">Stores</Link></>}{user?.role==='USER'&&<Link to="/stores">Stores</Link>}{user?.role==='STORE_OWNER'&&<Link to="/owner/dashboard">Dashboard</Link>}<Link to="/change-password">Change Password</Link><span className="userpill">{user?.name} · {user?.role}</span><button className="linkbtn" onClick={out}>Logout</button></div></nav>}
